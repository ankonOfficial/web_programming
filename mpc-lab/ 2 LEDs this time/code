void setup()
{
  for(int i=4; i<=13; i++)
  {
    pinMode(i, OUTPUT);
  }
}

void loop()
{
  for(int i=13; i>4; i--)
  {
    digitalWrite(i, HIGH);
    digitalWrite(i-1, HIGH);
    delay(200);
    digitalWrite(i, LOW);
    digitalWrite(i-1, LOW);
  }
}
